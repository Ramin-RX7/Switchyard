{
  "listen": ":8091",
  "backends": [
    { "url": "http://127.0.0.1:9001" },
    { "id": "secondary", "url": "http://127.0.0.1:9002" }
  ],
  "logging": {
    "outputs": ["console", "file"],
    "file": "switchyard.log",
    "format": "{receive_time} {method} {url} backend={backend_id} status={status} app_status={app_status} dur={request_duration} app_dur={app_duration} ua={req_header.User-Agent} resp_body={response_body}"
  }
}
